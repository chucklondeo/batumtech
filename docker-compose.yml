services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: batumtech
      POSTGRES_USER: batumtech
      POSTGRES_PASSWORD: change-me
    ports:
      - "5432:5432"
    volumes:
      - batumtech-postgres:/var/lib/postgresql/data

volumes:
  batumtech-postgres:

